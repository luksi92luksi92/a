# Backward-compatible dependency entry point.
# The stems-first pipeline now declares the complete runtime dependency set
# in requirements.txt; keep this file so existing Colab commands continue
# to install the full stack.
-r requirements.txt
